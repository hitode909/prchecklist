machine:
  node:
    version: 5.3.0
dependencies:
  override:
    - npm install
    - sbt clean
    - sbt test:compile
test:
  override:
    - sbt coverage core/test test
  post:
    - sbt core/coverageReport coverageReport coverageAggregate coveralls
